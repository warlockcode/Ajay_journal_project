<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.send_app.ajay_journal_project">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="util.JournalApi"
        android:allowBackup="true"
        android:icon="@drawable/image_one"
        android:label="@string/app_name"
        android:roundIcon="@drawable/image_one"
        android:supportsRtl="true"
        android:theme="@style/Theme.Ajay_journal_project">
        <activity android:name=".CommentActivity"
            android:windowSoftInputMode="adjustPan">
        </activity>
        <activity android:name=".LikesActivity" />
        <activity android:name=".FollowingUserActivity" />
        <activity android:name=".FollwedUsersActivity" />
        <activity android:name=".AccountUpadteActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".FollowActivity" />
        <activity android:name=".HomeAcivity" />
        <activity android:name=".JournalListActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".PostJournalActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".CreateAccountActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".LoginActivity"
            android:windowSoftInputMode="adjustPan"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>